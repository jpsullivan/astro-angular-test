---
import { Icon } from '@astrojs/starlight/components';
import '../styles/globals.css';
import ThemeSelect from '../components/ThemeSelect.astro';

const title = 'Modern Angular Components for Your SaaS';
const description = 'Production-ready, accessible Angular components powered by Spartan Brain. Build faster with TypeScript-first components designed for modern applications.';
---

<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{title}</title>
  <meta name="description" content={description} />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <style>
    @keyframes gradient-shift {
      0%, 100% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
    }

    @keyframes float {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-10px); }
    }

    @keyframes pulse-glow {
      0%, 100% { box-shadow: 0 0 20px rgba(147, 51, 234, 0.3); }
      50% { box-shadow: 0 0 40px rgba(147, 51, 234, 0.6); }
    }

    .gradient-text {
      background: linear-gradient(135deg,
        oklch(0.56 0.18 268) 0%,
        oklch(0.65 0.25 300) 50%,
        oklch(0.58 0.18 264) 100%
      );
      background-size: 200% 200%;
      animation: gradient-shift 8s ease infinite;
      -webkit-background-clip: text;
      background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .glass-card {
      background: oklch(1 0 0 / 0.05);
      backdrop-filter: blur(12px);
      border: 1px solid oklch(0.92 0.0035 39.5 / 0.2);
    }

    .dark .glass-card {
      background: oklch(0.2 0.02 283.51 / 0.3);
      border: 1px solid oklch(0.3 0.01 0 / 0.4);
    }

    .code-block {
      background: oklch(0.18 0.02 283.51);
      border: 1px solid oklch(0.3 0.01 0);
      font-family: 'SF Mono', Monaco, 'Cascadia Code', 'Roboto Mono', Consolas, 'Courier New', monospace;
    }

    .grid-pattern {
      background-image:
        linear-gradient(oklch(0.92 0.0035 39.5 / 0.1) 1px, transparent 1px),
        linear-gradient(90deg, oklch(0.92 0.0035 39.5 / 0.1) 1px, transparent 1px);
      background-size: 40px 40px;
    }

    .dark .grid-pattern {
      background-image:
        linear-gradient(oklch(0.3 0.01 0 / 0.2) 1px, transparent 1px),
        linear-gradient(90deg, oklch(0.3 0.01 0 / 0.2) 1px, transparent 1px);
    }

    .glow-on-hover {
      transition: all 0.3s ease;
    }

    .glow-on-hover:hover {
      box-shadow: 0 8px 32px oklch(0.56 0.18 268 / 0.3);
      transform: translateY(-2px);
    }

    .stat-card {
      transition: all 0.2s ease;
    }

    .stat-card:hover {
      transform: scale(1.05);
    }
  </style>
</head>
<body class="bg-background text-foreground">
  <!-- Navigation Header -->
  <header class="sticky top-0 z-50 w-full border-b border-border bg-background/80 backdrop-blur-sm">
    <div class="container mx-auto flex h-16 max-w-7xl items-center justify-between px-4 sm:px-6 lg:px-8">
      <div class="flex items-center gap-6">
        <a href="/" class="flex items-center gap-2 font-semibold text-lg">
          <span class="gradient-text">Component Docs</span>
        </a>
        <nav class="hidden md:flex gap-6">
          <a href="/guides/introduction/" class="text-sm font-medium text-muted-foreground hover:text-foreground transition-colors">Docs</a>
          <a href="/components/badge/" class="text-sm font-medium text-muted-foreground hover:text-foreground transition-colors">Components</a>
          <a href="/start/overview/" class="text-sm font-medium text-muted-foreground hover:text-foreground transition-colors">Get Started</a>
        </nav>
      </div>
      <div class="flex items-center gap-3">
        <ThemeSelect />
        <a href="https://github.com" target="_blank" rel="noopener" class="text-muted-foreground hover:text-foreground transition-colors">
          <Icon name="github" size="1.25rem" />
        </a>
      </div>
    </div>
  </header>

  <!-- Hero Section -->
  <section class="relative overflow-hidden grid-pattern py-20 sm:py-32">
    <div class="absolute inset-0 bg-gradient-to-b from-primary/5 to-transparent"></div>
    <div class="container relative mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="grid lg:grid-cols-2 gap-12 items-center">
        <!-- Left: Value Prop -->
        <div class="space-y-8">
          <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-primary/10 border border-primary/20 text-sm font-medium">
            <span class="relative flex h-2 w-2">
              <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-primary opacity-75"></span>
              <span class="relative inline-flex rounded-full h-2 w-2 bg-primary"></span>
            </span>
            Now with Angular 20 support
          </div>

          <h1 class="text-5xl sm:text-6xl lg:text-7xl font-bold tracking-tight">
            <span class="gradient-text">Modern Angular</span>
            <br />
            Components for
            <br />
            Your SaaS
          </h1>

          <p class="text-xl text-muted-foreground max-w-xl">
            Production-ready, accessible components built with Spartan Brain.
            TypeScript-first, tree-shakeable, and designed for developer happiness.
          </p>

          <div class="flex flex-wrap gap-4">
            <a href="/guides/introduction/"
               class="inline-flex items-center justify-center px-6 py-3 rounded-lg bg-primary text-primary-foreground font-medium hover:bg-primary/90 transition-all glow-on-hover">
              Get Started
              <Icon name="right-arrow" class="ml-2" size="1rem" />
            </a>
            <a href="/components/badge/"
               class="inline-flex items-center justify-center px-6 py-3 rounded-lg border border-border bg-background hover:bg-accent transition-all">
              Browse Components
              <Icon name="open-book" class="ml-2" size="1rem" />
            </a>
          </div>
        </div>

        <!-- Right: Code Preview -->
        <div class="relative">
          <div class="code-block rounded-xl p-6 shadow-2xl" style="animation: float 6s ease-in-out infinite;">
            <div class="flex items-center gap-2 mb-4 pb-3 border-b border-border/30">
              <div class="flex gap-1.5">
                <div class="w-3 h-3 rounded-full bg-[#ff5f56]"></div>
                <div class="w-3 h-3 rounded-full bg-[#ffbd2e]"></div>
                <div class="w-3 h-3 rounded-full bg-[#27c93f]"></div>
              </div>
              <span class="text-xs text-muted-foreground ml-2">app.component.ts</span>
            </div>
            <pre class="text-sm leading-relaxed"><code><span class="text-purple-400">import</span> <span class="text-yellow-300">&#123;</span> Component <span class="text-yellow-300">&#125;</span> <span class="text-purple-400">from</span> <span class="text-green-400">'@angular/core'</span><span class="text-gray-400">;</span>
<span class="text-purple-400">import</span> <span class="text-yellow-300">&#123;</span> BadgeComponent <span class="text-yellow-300">&#125;</span> <span class="text-purple-400">from</span> <span class="text-green-400">'./badge'</span><span class="text-gray-400">;</span>

<span class="text-gray-400">@</span><span class="text-yellow-300">Component</span><span class="text-gray-400">(&#123;</span>
  <span class="text-blue-300">selector</span><span class="text-gray-400">:</span> <span class="text-green-400">'app-root'</span><span class="text-gray-400">,</span>
  <span class="text-blue-300">standalone</span><span class="text-gray-400">:</span> <span class="text-purple-400">true</span><span class="text-gray-400">,</span>
  <span class="text-blue-300">imports</span><span class="text-gray-400">:</span> <span class="text-yellow-300">[</span>BadgeComponent<span class="text-yellow-300">]</span><span class="text-gray-400">,</span>
  <span class="text-blue-300">template</span><span class="text-gray-400">:</span> <span class="text-green-400">`
    &lt;h1&gt;Welcome&lt;/h1&gt;
    &lt;app-badge&gt;New&lt;/app-badge&gt;
  `</span>
<span class="text-gray-400">&#125;)</span>
<span class="text-purple-400">export</span> <span class="text-purple-400">class</span> <span class="text-yellow-300">AppComponent</span> <span class="text-gray-400">&#123;&#125;</span></code></pre>
          </div>
          <!-- Decorative gradient blob -->
          <div class="absolute -z-10 -top-10 -right-10 w-72 h-72 bg-primary/20 rounded-full blur-3xl"></div>
        </div>
      </div>
    </div>
  </section>

  <!-- Stats Bar -->
  <section class="border-y border-border">
    <div class="container mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-8">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-8">
        <div class="stat-card text-center">
          <div class="text-3xl font-bold gradient-text">12+</div>
          <div class="text-sm text-muted-foreground mt-1">Components</div>
        </div>
        <div class="stat-card text-center">
          <div class="text-3xl font-bold gradient-text">100%</div>
          <div class="text-sm text-muted-foreground mt-1">TypeScript</div>
        </div>
        <div class="stat-card text-center">
          <div class="text-3xl font-bold gradient-text">A11y</div>
          <div class="text-sm text-muted-foreground mt-1">Accessible</div>
        </div>
        <div class="stat-card text-center">
          <div class="text-3xl font-bold gradient-text">v20</div>
          <div class="text-sm text-muted-foreground mt-1">Angular</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Features Grid -->
  <section class="py-20 sm:py-32">
    <div class="container mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <h2 class="text-3xl sm:text-4xl font-bold mb-4">Built for Developers</h2>
        <p class="text-lg text-muted-foreground max-w-2xl mx-auto">
          Everything you need to build production-ready Angular applications with confidence.
        </p>
      </div>

      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
        <!-- Feature Card 1 -->
        <div class="glass-card rounded-xl p-6 glow-on-hover">
          <div class="w-12 h-12 rounded-lg bg-primary/10 flex items-center justify-center mb-4">
            <Icon name="seti:typescript" size="1.5rem" class="text-primary" />
          </div>
          <h3 class="text-xl font-semibold mb-2">TypeScript First</h3>
          <p class="text-muted-foreground">
            Fully typed with comprehensive type definitions. IntelliSense support out of the box.
          </p>
        </div>

        <!-- Feature Card 2 -->
        <div class="glass-card rounded-xl p-6 glow-on-hover">
          <div class="w-12 h-12 rounded-lg bg-primary/10 flex items-center justify-center mb-4">
            <Icon name="puzzle" size="1.5rem" class="text-primary" />
          </div>
          <h3 class="text-xl font-semibold mb-2">Standalone Components</h3>
          <p class="text-muted-foreground">
            Built with Angular's modern standalone API. No modules required, just import and use.
          </p>
        </div>

        <!-- Feature Card 3 -->
        <div class="glass-card rounded-xl p-6 glow-on-hover">
          <div class="w-12 h-12 rounded-lg bg-primary/10 flex items-center justify-center mb-4">
            <Icon name="rocket" size="1.5rem" class="text-primary" />
          </div>
          <h3 class="text-xl font-semibold mb-2">Spartan Brain Powered</h3>
          <p class="text-muted-foreground">
            Built on solid primitives from @spartan-ng/brain for maximum flexibility and composability.
          </p>
        </div>

        <!-- Feature Card 4 -->
        <div class="glass-card rounded-xl p-6 glow-on-hover">
          <div class="w-12 h-12 rounded-lg bg-primary/10 flex items-center justify-center mb-4">
            <Icon name="approve-check" size="1.5rem" class="text-primary" />
          </div>
          <h3 class="text-xl font-semibold mb-2">Accessible by Default</h3>
          <p class="text-muted-foreground">
            WCAG 2.1 AA compliant. Keyboard navigation, screen readers, and ARIA attributes included.
          </p>
        </div>

        <!-- Feature Card 5 -->
        <div class="glass-card rounded-xl p-6 glow-on-hover">
          <div class="w-12 h-12 rounded-lg bg-primary/10 flex items-center justify-center mb-4">
            <Icon name="setting" size="1.5rem" class="text-primary" />
          </div>
          <h3 class="text-xl font-semibold mb-2">Highly Customizable</h3>
          <p class="text-muted-foreground">
            Tailwind CSS integration with full theming support. Light and dark modes included.
          </p>
        </div>

        <!-- Feature Card 6 -->
        <div class="glass-card rounded-xl p-6 glow-on-hover">
          <div class="w-12 h-12 rounded-lg bg-primary/10 flex items-center justify-center mb-4">
            <Icon name="star" size="1.5rem" class="text-primary" />
          </div>
          <h3 class="text-xl font-semibold mb-2">Production Ready</h3>
          <p class="text-muted-foreground">
            Battle-tested components used in real-world SaaS applications. Ship with confidence.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Quick Start Section -->
  <section class="py-20 sm:py-32 bg-muted/30">
    <div class="container mx-auto max-w-5xl px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="text-3xl sm:text-4xl font-bold mb-4">Get Started in Seconds</h2>
        <p class="text-lg text-muted-foreground">
          Install and start using components in your Angular project right away.
        </p>
      </div>

      <div class="space-y-8">
        <!-- Step 1 -->
        <div class="flex gap-4 items-start">
          <div class="flex-shrink-0 w-8 h-8 rounded-full bg-primary text-primary-foreground flex items-center justify-center font-bold text-sm">
            1
          </div>
          <div class="flex-1">
            <h3 class="font-semibold mb-2 text-lg">Install the package</h3>
            <div class="code-block rounded-lg p-4 font-mono text-sm overflow-x-auto">
              <span class="text-green-400">npm install</span> <span class="text-blue-300">@your-org/angular-components</span>
            </div>
          </div>
        </div>

        <!-- Step 2 -->
        <div class="flex gap-4 items-start">
          <div class="flex-shrink-0 w-8 h-8 rounded-full bg-primary text-primary-foreground flex items-center justify-center font-bold text-sm">
            2
          </div>
          <div class="flex-1">
            <h3 class="font-semibold mb-2 text-lg">Import components</h3>
            <div class="code-block rounded-lg p-4 font-mono text-sm overflow-x-auto">
              <span class="text-purple-400">import</span> <span class="text-yellow-300">&#123;</span> <span class="text-blue-300">BadgeComponent</span> <span class="text-yellow-300">&#125;</span> <span class="text-purple-400">from</span> <span class="text-green-400">'@your-org/angular-components'</span><span class="text-gray-400">;</span>
            </div>
          </div>
        </div>

        <!-- Step 3 -->
        <div class="flex gap-4 items-start">
          <div class="flex-shrink-0 w-8 h-8 rounded-full bg-primary text-primary-foreground flex items-center justify-center font-bold text-sm">
            3
          </div>
          <div class="flex-1">
            <h3 class="font-semibold mb-2 text-lg">Use in your templates</h3>
            <div class="code-block rounded-lg p-4 font-mono text-sm overflow-x-auto">
              <span class="text-gray-400">&lt;</span><span class="text-blue-300">app-badge</span> <span class="text-yellow-300">variant</span><span class="text-gray-400">=</span><span class="text-green-400">"secondary"</span><span class="text-gray-400">&gt;</span>New Feature<span class="text-gray-400">&lt;/</span><span class="text-blue-300">app-badge</span><span class="text-gray-400">&gt;</span>
            </div>
          </div>
        </div>
      </div>

      <div class="mt-12 text-center">
        <a href="/guides/introduction/"
           class="inline-flex items-center justify-center px-6 py-3 rounded-lg bg-primary text-primary-foreground font-medium hover:bg-primary/90 transition-all">
          Read Full Documentation
          <Icon name="right-arrow" class="ml-2" size="1rem" />
        </a>
      </div>
    </div>
  </section>

  <!-- Component Preview -->
  <section class="py-20 sm:py-32">
    <div class="container mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <h2 class="text-3xl sm:text-4xl font-bold mb-4">Explore Components</h2>
        <p class="text-lg text-muted-foreground max-w-2xl mx-auto">
          Browse our growing library of production-ready components with live examples and code snippets.
        </p>
      </div>

      <div class="grid md:grid-cols-2 gap-8 max-w-4xl mx-auto">
        <!-- Badge Preview -->
        <a href="/components/badge/" class="glass-card rounded-xl p-8 hover:border-primary/50 transition-all glow-on-hover block">
          <div class="flex items-center justify-between mb-4">
            <h3 class="text-xl font-semibold">Badge</h3>
            <Icon name="right-arrow" size="1rem" />
          </div>
          <p class="text-muted-foreground mb-6">Display status, labels, or counts with multiple variants.</p>
          <div class="flex flex-wrap gap-2">
            <span class="inline-flex items-center px-3 py-1 rounded-md text-xs font-medium bg-primary text-primary-foreground">Default</span>
            <span class="inline-flex items-center px-3 py-1 rounded-md text-xs font-medium bg-secondary text-secondary-foreground">Secondary</span>
            <span class="inline-flex items-center px-3 py-1 rounded-md text-xs font-medium border border-border">Outline</span>
          </div>
        </a>

        <!-- Input Preview -->
        <a href="/components/input/" class="glass-card rounded-xl p-8 hover:border-primary/50 transition-all glow-on-hover block">
          <div class="flex items-center justify-between mb-4">
            <h3 class="text-xl font-semibold">Input</h3>
            <Icon name="right-arrow" size="1rem" />
          </div>
          <p class="text-muted-foreground mb-6">Form inputs with validation, error states, and accessibility.</p>
          <input
            type="text"
            placeholder="Enter your email..."
            class="w-full px-3 py-2 rounded-lg border border-border bg-background focus:outline-none focus:ring-2 focus:ring-primary/50"
            readonly
          />
        </a>
      </div>

      <div class="text-center mt-12">
        <a href="/components/badge/"
           class="inline-flex items-center justify-center px-6 py-3 rounded-lg border border-border bg-background hover:bg-accent transition-all">
          View All Components
          <Icon name="open-book" class="ml-2" size="1rem" />
        </a>
      </div>
    </div>
  </section>

  <!-- Final CTA -->
  <section class="relative overflow-hidden py-20 sm:py-32 grid-pattern">
    <div class="absolute inset-0 bg-gradient-to-t from-primary/5 to-transparent"></div>
    <div class="container relative mx-auto max-w-4xl px-4 sm:px-6 lg:px-8 text-center">
      <h2 class="text-4xl sm:text-5xl font-bold mb-6">
        Ready to Build Something Great?
      </h2>
      <p class="text-xl text-muted-foreground mb-8 max-w-2xl mx-auto">
        Join developers building modern SaaS applications with our component library.
      </p>
      <div class="flex flex-wrap gap-4 justify-center">
        <a href="/guides/introduction/"
           class="inline-flex items-center justify-center px-8 py-4 rounded-lg bg-primary text-primary-foreground font-medium text-lg hover:bg-primary/90 transition-all glow-on-hover">
          Get Started Now
          <Icon name="right-arrow" class="ml-2" size="1.25rem" />
        </a>
        <a href="https://github.com"
           target="_blank"
           rel="noopener"
           class="inline-flex items-center justify-center px-8 py-4 rounded-lg border border-border bg-background hover:bg-accent transition-all text-lg font-medium">
          <Icon name="github" class="mr-2" size="1.25rem" />
          View on GitHub
        </a>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="border-t border-border py-12">
    <div class="container mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="grid md:grid-cols-4 gap-8">
        <div>
          <div class="font-semibold mb-4 gradient-text">Component Docs</div>
          <p class="text-sm text-muted-foreground">Modern Angular components for your SaaS.</p>
        </div>
        <div>
          <div class="font-semibold mb-4">Documentation</div>
          <ul class="space-y-2 text-sm">
            <li><a href="/guides/introduction/" class="text-muted-foreground hover:text-foreground transition-colors">Getting Started</a></li>
            <li><a href="/components/badge/" class="text-muted-foreground hover:text-foreground transition-colors">Components</a></li>
            <li><a href="/start/overview/" class="text-muted-foreground hover:text-foreground transition-colors">Guides</a></li>
          </ul>
        </div>
        <div>
          <div class="font-semibold mb-4">Resources</div>
          <ul class="space-y-2 text-sm">
            <li><a href="#" class="text-muted-foreground hover:text-foreground transition-colors">Examples</a></li>
            <li><a href="#" class="text-muted-foreground hover:text-foreground transition-colors">Blog</a></li>
            <li><a href="#" class="text-muted-foreground hover:text-foreground transition-colors">Changelog</a></li>
          </ul>
        </div>
        <div>
          <div class="font-semibold mb-4">Community</div>
          <ul class="space-y-2 text-sm">
            <li><a href="#" class="text-muted-foreground hover:text-foreground transition-colors">GitHub</a></li>
            <li><a href="#" class="text-muted-foreground hover:text-foreground transition-colors">Discord</a></li>
            <li><a href="#" class="text-muted-foreground hover:text-foreground transition-colors">Twitter</a></li>
          </ul>
        </div>
      </div>
      <div class="mt-12 pt-8 border-t border-border text-center text-sm text-muted-foreground">
        <p>© 2025 Component Docs. Built with Angular and Astro.</p>
      </div>
    </div>
  </footer>

  <script>
    // Initialize theme on page load
    const theme = localStorage.getItem('starlight-theme') || 'auto';
    const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;

    if (theme === 'dark' || (theme === 'auto' && prefersDark)) {
      document.documentElement.classList.add('dark');
    } else {
      document.documentElement.classList.remove('dark');
    }

    // Listen for theme changes
    window.addEventListener('storage', (e) => {
      if (e.key === 'starlight-theme') {
        const newTheme = e.newValue || 'auto';
        const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;

        if (newTheme === 'dark' || (newTheme === 'auto' && prefersDark)) {
          document.documentElement.classList.add('dark');
        } else {
          document.documentElement.classList.remove('dark');
        }
      }
    });
  </script>
</body>
</html>
